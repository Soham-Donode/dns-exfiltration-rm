# Models

Model training notebooks, experiment scripts, evaluation metrics, and model checkpoints.

> **Note:** Large model weight files (`*.pkl`, `*.h5`, `*.onnx`, `*.pt`) should be managed via `.gitignore` or Git LFS.
